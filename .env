# Ejemplo de configuración para SQL Server con autenticación SQL (Usuario/Contraseña)
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa                      # Tu usuario de SQL Server (sa)
DB_PASSWORD=Kitanchorra_90 # Tu contraseña (la que ingresaste)
DB_DATABASE=SistemaSeguridadElectronica
# Agrega cualquier otra variable de entorno que uses (como puerto, secret, etc.)